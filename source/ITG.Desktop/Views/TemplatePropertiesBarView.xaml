<UserControl x:Class="ITG.Desktop.Views.TemplatePropertiesBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="435">
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Stretch">
        <TextBlock Text="Template Name: " Margin="5 0 0 0"/>
        <TextBox Text="{Binding Name}" Width="150" Margin="0 0 30 0"/>
        <TextBlock Text="Width: "/>
        <TextBox Text="{Binding Width}" Width="32" Margin="0 0 15 0"/>
        <TextBlock Text="Height: "/>
        <TextBox Text="{Binding Height}" Width="32" Margin="0 0 5 0"/>

    </StackPanel>
</UserControl>
